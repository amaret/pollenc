#
# Upstart Ubuntu script for CLC Msp430 Environment
#
# temporarily just starting one of these until we have a daemon mgt plan
#

start on runlevel [2345]
stop on runlevel [06]

script
  exec start-stop-daemon --start --quiet --chuid root --exec /home/windops/wind.io/pollenc/PollencServer.py -- -n $UPSTART_JOB;
end script

