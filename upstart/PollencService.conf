#
# Upstart Ubuntu script for CLC Msp430 Environment
#
# temporarily just starting one of these until we have a daemon mgt plan
#

start on runlevel [2345]
stop on runlevel [06]

script
  exec start-stop-daemon --start --quiet --chuid root --exec /home/windops/git/pollenc/PollencServer.py -- --rdishost redbis.wind.io --rdisport 6379;
end script

