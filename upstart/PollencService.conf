#
# Upstart Ubuntu script for server
#
# temporarily just starting one of these until we have a daemon mgt plan
#

start on runlevel [2345]
stop on runlevel [06]

script
  exec start-stop-daemon --start --quiet --chuid root --exec /home/pops/pollenc/PollencServer.py;
end script

